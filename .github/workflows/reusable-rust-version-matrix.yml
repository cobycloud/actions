name: Reusable Rust Version Matrix

on:
  workflow_call:
    inputs:
      runners:
        type: string
        required: false
        default: '["ubuntu-latest"]'
      rust-toolchains:
        type: string
        required: false
        default: '["stable","beta","nightly"]'
      working-directory:
        type: string
        required: false
        default: "."
      components:
        type: string
        required: false
        default: rustfmt,clippy
      targets:
        type: string
        required: false
        default: ""
      fail-fast:
        type: boolean
        required: false
        default: false
      fmt-command:
        type: string
        required: false
        default: cargo fmt --all -- --check
      clippy-command:
        type: string
        required: false
        default: cargo clippy --all-targets --all-features -- -D warnings
      test-command:
        type: string
        required: false
        default: cargo test --all-features
      build-command:
        type: string
        required: false
        default: cargo build --all-features
      doc-command:
        type: string
        required: false
        default: ""

permissions:
  contents: read

jobs:
  rust-matrix:
    strategy:
      fail-fast: ${{ inputs.fail-fast }}
      matrix:
        runner: ${{ fromJSON(inputs.runners) }}
        rust-toolchain: ${{ fromJSON(inputs.rust-toolchains) }}
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v5

      - uses: cobycloud/actions/actions/rust-cargo-ci@main
        with:
          rust-toolchain: ${{ matrix.rust-toolchain }}
          working-directory: ${{ inputs.working-directory }}
          components: ${{ inputs.components }}
          targets: ${{ inputs.targets }}
          fmt-command: ${{ inputs.fmt-command }}
          clippy-command: ${{ inputs.clippy-command }}
          test-command: ${{ inputs.test-command }}
          build-command: ${{ inputs.build-command }}
          doc-command: ${{ inputs.doc-command }}
